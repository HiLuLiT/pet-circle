{
  "name": "pet-circle-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project pet-circle-app"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "resend": "^4.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "typescript": "^5.7.0"
  }
}
